So from the AI development stack we know that BMAD will do the applications and the idea of POM is to create prompts, prompt templates and also data sources and schemas.
Now like BMAD method it will have a core, we'll call it POM Core. It will also have an application and the application is generally for visualization and for fast prompt template creation.
The output won't be an app directory or a dots directory, it will actually be a folder called POM.
